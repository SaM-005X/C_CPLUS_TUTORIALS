#include<stdio.h>
#include<stdbool.h>

int main(){

    // 1) logical operators = && (and) checks if two conditions are true:->

    // float temp = 25;
    // bool sunny = true;

    // if(temp >= 0 && temp <= 30 && sunny){
    //     printf("\nthe whether is good!");
    // }
    // else{
    //     printf("\nthe whether is bad!");
    // }
    // return 0;

    // 2) logical operators = || (or) checks if at least one condition is true:->

    // float temp = 25;

    // if(temp <= 0 || temp >= 30){
    //     printf("\nthe whether is bad!");
    // }
    // else{
    //     printf("\nthe whether is good!");
    // }
    // return 0;

    // 3) logical operators = ! (not) reverses the state of a condition

    // bool sunny = false;

    // if(!sunny){
    //     printf("\n it's cloudy outside!");
    // }
    // else{
    //     printf("\nit's sunny outside!");
    // }
    // return 0;
}